#include <stdio.h>
#include <dirent.h>
#include <iostream>
#include <vector>

using namespace std;
vector<string> files = {};



int main(int argc, const char**argv) {
       /*
    struct dirent *entry = nullptr;
    DIR *dp = nullptr;
    dp = opendir("/home/yoy/Desktop/furniture_customization_for_sugar_daddy/pigmentation/images");
    if (dp != nullptr) {
        while ((entry = readdir(dp))){
            files.push_back(entry->d_name);
        }
    }

    // First two elemetns are .. and .
    files.erase(files.begin());
    files.erase(files.begin());
 
    for (int i = 2; i<files.size() ; i++) {
    cout << files[i] << "\n";
    }
    cout << files.size() << "\n";
    */

    return 0;
}